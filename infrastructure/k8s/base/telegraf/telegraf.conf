[agent]
  interval = "10s"
  round_interval = true
  metric_batch_size = 1000
  metric_buffer_limit = 10000
  collection_jitter = "0s"
  flush_interval = "10s"
  flush_jitter = "0s"
  precision = ""
  debug = true   
  quiet = false
  logfile = ""
  hostname = ""
  omit_hostname = false

[[inputs.mqtt_consumer]]
  # Changed from mqtt-broker to mqtt to match your docker-compose service name
  servers = ["tcp://mqtt:1883"] 
  topics = ["fire/sensors/#"] 
  data_format = "json_v2"
  name_override = "fire_data"

  [[inputs.mqtt_consumer.json_v2]]
    [[inputs.mqtt_consumer.json_v2.object]]
      path = "@this"
      tags = ["h_id"]
      [inputs.mqtt_consumer.json_v2.object.fields]
        "lat" = "float"
        "lon" = "float"
        "status" = "int"

[[outputs.influxdb_v2]]
  # Changed from influxdb to influx to match your docker-compose service name
  urls = ["http://influx:8086"] 
  token = "${INFLUXDB_TOKEN}" 
  organization = "fire-monitoring" 
  bucket = "sensor-data"