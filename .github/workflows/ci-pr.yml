name: Deprecated PR Validation

on:
<<<<<<< Updated upstream
  pull_request:
    branches:
      - main
    paths:
      - "api/**"
      - "etl-processor/**"
      - "infrastructure/**"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
=======
  workflow_dispatch:
>>>>>>> Stashed changes

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
<<<<<<< Updated upstream
          npm run --if-present lint
          npm run --if-present test

  etl-validation:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: etl-processor/requirements.txt

      - name: Install ETL dependencies
        working-directory: etl-processor
        run: pip install -r requirements.txt

      - name: Validate ETL source
        working-directory: etl-processor
        run: python -m compileall src

  terraform-validation:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash
        working-directory: infrastructure/terraform
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform fmt
        run: terraform fmt -check

      - name: Terraform init (no backend)
        run: terraform init -backend=false -input=false

      - name: Terraform validate
        run: terraform validate
=======
          echo "This workflow is deprecated."
          echo "Use .github/workflows/app-ci-build.yml and .github/workflows/terraform-infra.yml instead."
>>>>>>> Stashed changes
