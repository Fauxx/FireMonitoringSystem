name: "Terraform PR Plan Review"

on:
  pull_request:
    paths:
      - 'infrastructure/terraform/environments/gke-dev/**'
      - 'infrastructure/terraform/environments/gke-prod/**'
      - '.github/workflows/terraform-pr-plan.yml'

permissions:
  contents:      read
  id-token:      write
  pull-requests: write

concurrency:
  group: tf-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  detect_layers:
    name: "Detect Changed TF Layers"
    runs-on: ubuntu-latest
    outputs:
      layers: ${{ steps.set_layers.outputs.layers }}
      any_changed: ${{ steps.set_layers.outputs.any_changed }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            dev_infra:
              - 'infrastructure/terraform/environments/gke-dev/01-infra/**'
            dev_platform:
              - 'infrastructure/terraform/environments/gke-dev/02-platform/**'
            dev_argocd:
              - 'infrastructure/terraform/environments/gke-dev/03-argocd/**'
            prod_infra:
              - 'infrastructure/terraform/environments/gke-prod/01-infra/**'
            prod_platform:
              - 'infrastructure/terraform/environments/gke-prod/02-platform/**'
            prod_argocd:
              - 'infrastructure/terraform/environments/gke-prod/03-argocd/**'

      - name: Build Changed Layer Matrix
        id: set_layers
        run: |
          LAYERS='[]'
          if [ "${{ steps.filter.outputs.dev_infra }}" = "true" ]; then LAYERS=$(echo "$LAYERS" | jq -c '. + [{"env":"gke-dev","layer":"01-infra","path":"infrastructure/terraform/environments/gke-dev/01-infra"}]'); fi
          if [ "${{ steps.filter.outputs.dev_platform }}" = "true" ]; then LAYERS=$(echo "$LAYERS" | jq -c '. + [{"env":"gke-dev","layer":"02-platform","path":"infrastructure/terraform/environments/gke-dev/02-platform"}]'); fi
          if [ "${{ steps.filter.outputs.dev_argocd }}" = "true" ]; then LAYERS=$(echo "$LAYERS" | jq -c '. + [{"env":"gke-dev","layer":"03-argocd","path":"infrastructure/terraform/environments/gke-dev/03-argocd"}]'); fi
          if [ "${{ steps.filter.outputs.prod_infra }}" = "true" ]; then LAYERS=$(echo "$LAYERS" | jq -c '. + [{"env":"gke-prod","layer":"01-infra","path":"infrastructure/terraform/environments/gke-prod/01-infra"}]'); fi
          if [ "${{ steps.filter.outputs.prod_platform }}" = "true" ]; then LAYERS=$(echo "$LAYERS" | jq -c '. + [{"env":"gke-prod","layer":"02-platform","path":"infrastructure/terraform/environments/gke-prod/02-platform"}]'); fi
          if [ "${{ steps.filter.outputs.prod_argocd }}" = "true" ]; then LAYERS=$(echo "$LAYERS" | jq -c '. + [{"env":"gke-prod","layer":"03-argocd","path":"infrastructure/terraform/environments/gke-prod/03-argocd"}]'); fi
          
          if [ "$LAYERS" = "[]" ] || [ -z "$LAYERS" ]; then
            echo "any_changed=false" >> $GITHUB_OUTPUT
            echo "layers=[]"         >> $GITHUB_OUTPUT
          else
            echo "any_changed=true"   >> $GITHUB_OUTPUT
            echo "layers=$LAYERS"     >> $GITHUB_OUTPUT
          fi

  plan:
    name: "TF Plan: ${{ matrix.env }}/${{ matrix.layer }}"
    needs: detect_layers
    if: needs.detect_layers.outputs.any_changed == 'true'
    runs-on: ubuntu-latest
    environment: ${{ matrix.env }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.detect_layers.outputs.layers) }}

    env:
      TF_VAR_gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
      TF_VAR_github_owner:          ${{ github.repository_owner }}
      TF_VAR_github_repository:     ${{ github.event.repository.name }}
      TF_VAR_github_app_id:               ${{ secrets.APP_ID }}
      TF_VAR_github_app_installation_id:  ${{ secrets.APP_INSTALLATION_ID }}
      TF_VAR_github_app_private_key:      ${{ secrets.APP_PRIVATE_KEY }}
      TF_VAR_cloudflare_tunnel_credentials_json: ${{ secrets.CLOUDFLARE_TUNNEL_CREDENTIALS_JSON }}

    defaults:
      run:
        working-directory: ${{ matrix.path }}

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account:            ${{ vars.GCP_SERVICE_ACCOUNT }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ~> 1.11.0
          terraform_wrapper: true

      - name: Setup kubelogin
        uses: azure/use-kubelogin@v1
        with:
          kubelogin-version: 'v0.1.7'

      - name: Terraform Init
        id: init
        run: terraform init -reconfigure -backend-config=backend.conf

      - name: Terraform Format Check
        id: fmt
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color
        continue-on-error: true

      - name: Terraform Plan
        id: plan
        run: terraform plan -no-color -refresh=true
        continue-on-error: true

      - name: Post Sticky Plan Comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: tf-plan-${{ matrix.env }}-${{ matrix.layer }}
          message: |
            ## 🏗️ Terraform Plan — `${{ matrix.env }}/${{ matrix.layer }}`

            | Step | Status |
            |------|--------|
            | fmt      | ${{ steps.fmt.outcome == 'success' && '✅ Passed' || '❌ Failed' }} |
            | validate | ${{ steps.validate.outcome == 'success' && '✅ Passed' || '❌ Failed' }} |
            | plan     | ${{ steps.plan.outcome == 'success' && '✅ No errors' || '❌ Has errors' }} |

            <details><summary>📋 Plan Output</summary>

            ```
            ${{ steps.plan.outputs.stdout }}
            ```
            </details>
