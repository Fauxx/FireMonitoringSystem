name: test
on: push
jobs:
  test:
    uses: ./.github/workflows/gcp-terraform-reusable.yml
    with:
      environment: gke-dev
      layer_dir: infrastructure/terraform/environments/gke-dev/00-bootstrap
      execution_mode: apply
    secrets: inherit
