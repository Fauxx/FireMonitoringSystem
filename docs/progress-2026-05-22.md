# Progress Report - May 22, 2026

## 1. Resolved Issues

### Reusable Workflow Parser Errors
- **File**: [.github/workflows/terraform-reusable.yml](file:///home/zett/RiderProjects/FireMonitoringSystem/.github/workflows/terraform-reusable.yml)
- **Fixes**:
  - Removed invalid job-level permissions `environments: write` and `secrets: write`.
  - Moved the `TF_VAR_github_token` environment variable declaration from the job-level block into the step-level `env` blocks for `Execute Contract (Backend Initialization)` and `Execute Immutable Apply`, resolving the unrecognized `steps` context named-value error.
  - Corrected the typo in the `terraform apply` step from `tfplan.binarypprove tfplan.binary` to `tfplan.binary`.

### Deploy Orchestrator Parser Errors
- **File**: [.github/workflows/terraform-deploy.yml](file:///home/zett/RiderProjects/FireMonitoringSystem/.github/workflows/terraform-deploy.yml)
- **Fixes**:
  - Removed invalid workflow-level permissions `environments: write` and `secrets: write` that were causing orchestrator parsing freezes.

### Secret Gating on Validation
- **File**: [.github/workflows/terraform-reusable.yml](file:///home/zett/RiderProjects/FireMonitoringSystem/.github/workflows/terraform-reusable.yml)
- **Fixes**:
  - Reverted the conditional environment bypass on validation. The job environment is now set to always use `environment: ${{ inputs.env }}` instead of resolving to an empty string (`''`) on validate mode. This allows the validation steps to successfully load environment-level secrets (`DO_TOKEN`, `TF_STATE_ACCESS_KEY`, `TF_STATE_SECRET_KEY`) required to initialize the backend and generate the plan.

---

## 2. Infrastructure & Git Actions Taken
1. Merged `origin/main` into `dev-zet`.
2. Committed and pushed syntax corrections for both `terraform-reusable.yml` and `terraform-deploy.yml`.
3. Created the `prod` environment in the GitHub repository using the API.

---

## 3. Current Pipeline Status
- The validation check for `dev` successfully accesses the `dev` environment secrets.
- The validation check for `prod` is currently failing with `Missing bootstrap-required secret(s) for infra: DO_TOKEN` because the `prod` environment secrets have not been configured in the GitHub repository settings.

---

## 4. Next Steps
- Add the necessary secrets (`DO_TOKEN`, `TF_STATE_ACCESS_KEY`, `TF_STATE_SECRET_KEY`) under the `prod` environment settings in the GitHub repository UI, or alternatively migrate them to repository-wide secrets so both environments can inherit them.
