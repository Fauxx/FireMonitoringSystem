apiVersion: v1
kind: Secret
metadata:
  name: fire-monitoring-secrets
type: Opaque
stringData:
  # ──────────────────────────────────────────────────────────
  # BASE TEMPLATE: These are placeholder values only.
  # Real secrets are injected by environment-specific overlays:
  #   overlays/local/secrets.yaml  (local dev - gitignored)
  #   overlays/dev/secrets.yaml    (GKE dev   - gitignored)
  #   overlays/prod/secrets.yaml   (GKE prod  - gitignored)
  # ──────────────────────────────────────────────────────────
  POSTGRES_PASSWORD: "PLACEHOLDER_OVERRIDE_IN_OVERLAY"
  DATABASE_URL: "PLACEHOLDER_OVERRIDE_IN_OVERLAY"
  INFLUXDB_TOKEN: "PLACEHOLDER_OVERRIDE_IN_OVERLAY"
  DOCKER_INFLUXDB_INIT_PASSWORD: "PLACEHOLDER_OVERRIDE_IN_OVERLAY"
  DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: "PLACEHOLDER_OVERRIDE_IN_OVERLAY"
