apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-tunnel-credentials
type: Opaque
stringData:
  # This is a placeholder — the real credentials.json is injected
  # via the Makefile target: make cloudflared-secret
  # DO NOT commit the real credentials.json to Git.
  credentials.json: |
    REPLACE_WITH_BASE64_ENCODED_CREDENTIALS
