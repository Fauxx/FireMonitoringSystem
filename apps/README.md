# Apps Hub

Application services are now grouped under `apps/`.

## Current service locations

1. `apps/api/`
2. `apps/dashboard/`
3. `apps/etl-processor/`
4. `apps/simulators/`

## Notes

1. Image names and deployment manifests remain unchanged.
2. CI paths and build contexts were updated to track these locations.
