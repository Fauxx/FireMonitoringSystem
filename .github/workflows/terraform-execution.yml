name: Deprecated Terraform Execution Reusable

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/terraform-infra.yml instead."
