# Tests Hub (Migration Scaffold)

This directory is reserved for repository-level integration and end-to-end tests.

## Intended layout

1. `tests/integration/`
2. `tests/e2e/`
3. Shared test fixtures and compose test harness.

Current service-level tests can remain inside each service until migration phases define consolidation.
